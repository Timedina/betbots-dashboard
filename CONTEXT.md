
## Atualização 12/08/2026 (cont.) — auditoria de segurança Supabase + melhorias no dashboard

- **Auditoria de seguranca (Supabase advisors via MCP)**: RLS ja estava habilitado nas 11 tabelas do projeto (pendencia antiga de apostas/metricas/historical_odds ja tinha sido resolvida, so nao estava documentada); achado novo: 5 views (v_apostas_por_segmento, v_apostas_resumo, v_apostas_curva_pnl, v_apostas_drawdown_maximo, v_apostas_slippage) estavam com SECURITY DEFINER (nivel ERROR), rodando com privilegio de quem criou a view e ignorando o RLS de quem consulta.
- **Fix**: ALTER VIEW ... SET (security_invoker = true) nas 5 views + funcao atualizar_timestamp corrigida com SET search_path = public (resolvia alerta WARN de search_path mutavel); advisor de seguranca reconfirmado limpo (zero alertas) apos os dois fixes, tudo aplicado via Supabase MCP.
- **Pendencia real restante**: rotacionar ODDSPAPI_API_KEY e SUPABASE_SERVICE_KEY (expostas em chat em sessoes anteriores) — acao manual do usuario nos dois paineis + atualizar .env no VPS + restart dos servicos, ainda nao feito.

- **Melhorias no dashboard (EM ANDAMENTO)**: pesquisa de UX concluida, dashboard classificado como operacional+analitico de monitoramento solo; 7 melhorias priorizadas: (1) timeline de incidentes sobre PnL, (2) widget de saude, (3) Supabase Realtime em vez de polling, (4) virtualizacao da tabela de analises (13k+ linhas), (5) codificacao visual das flags no_limite/sombra_*, (6) filtros persistidos na URL, (7) drill-down na segmentacao.
- **Dashboard**: vive em ~/betbots-dashboard no VPS (repo GitHub Timedina/betbots-dashboard, separado do repo do bot); stack React 19 + Vite 8, sem @supabase/supabase-js (fetch cru na REST API com chave anon, protegida por RLS) e sem lib de grafico; tudo num App.jsx monolitico (~900 linhas) com abas analises/grafico/apostas/filtros/chat/estrategias.
- **Item 1 (timeline de incidentes) CONCLUIDO**: aba Grafico ganhou coluna "Evento" com badge e tooltip nos dias com incidente documentado (dict INCIDENTES hardcoded, sem dependencia nova); build validado limpo, confirmado por screenshot; commit ad37c08, push final edb83f0.
- **Item 2 (widget de saude) EM ANDAMENTO**: criada tabela saude_bots no Supabase via MCP (bot_id FK + integracao text, PK composta, colunas status/ok_streak/fail_streak/ultimo_ok/ultimo_erro/atualizado_em), RLS com mesma politica de leitura publica de metricas; falta patch em saude.py (VPS) para gravar la tambem (hoje so grava dados_bot/saude.json local) e o componente no dashboard.
- **Itens 3-7**: ainda nao iniciados.
